Place labeled pomegranate images in the class folders under leaf/ and fruit/.

Do not train on the five Gallery demo photos. Those are for inference testing only
(Diseases → AI Disease Detection, or Gallery → Analyze).

Aim for hundreds of representative images per class, with separate train / val / test
splits and no near-duplicates across splits.
